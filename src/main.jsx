import React from "react";
import ReactDOM from "react-dom/client";
import TwitterSearchBuilder from "./TwitterSearchBuilder";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TwitterSearchBuilder />
  </React.StrictMode>
);
